services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: kyma
      POSTGRES_PASSWORD: kyma_dev
      POSTGRES_DB: kyma
    ports:
      - "5432:5432"
    volumes:
      - kyma_pgdata:/var/lib/postgresql/data

volumes:
  kyma_pgdata:
